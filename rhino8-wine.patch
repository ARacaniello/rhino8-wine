diff --git a/dlls/ntdll/unix/thread.c b/dlls/ntdll/unix/thread.c
index 546963a4241..a49658b54e0 100644
--- a/dlls/ntdll/unix/thread.c
+++ b/dlls/ntdll/unix/thread.c
@@ -1199,8 +1199,10 @@ NTSTATUS init_thread_stack( TEB *teb, ULONG_PTR limit, SIZE_T reserve_size, SIZE
         teb->Tib.StackLimit = stack.StackLimit;
         teb->DeallocationStack = stack.DeallocationStack;
 
-        /* 32-bit stack */
+        /* 32-bit stack - require at least 8 MB so .NET 8 initialization does not
+         * overflow; the PE header default of 1 MB is insufficient on Wine. */
         if (!limit || limit > user_space_wow_limit) limit = user_space_wow_limit;
+        if (reserve_size < 8 * 1024 * 1024) reserve_size = 8 * 1024 * 1024;
         if ((status = virtual_alloc_thread_stack( &stack, 0, limit, reserve_size, commit_size, TRUE )))
             return status;
         wow_teb->Tib.StackBase = PtrToUlong( stack.StackBase );
@@ -1232,11 +1234,26 @@ NTSTATUS init_thread_stack( TEB *teb, ULONG_PTR limit, SIZE_T reserve_size, SIZE
     }
 #endif
 
-    /* native stack */
+    /* native stack: reserve 512 MB so any Wine overhead fits without overflow.
+     * The .NET 8 CLR calibrates its recursion depth to (StackBase - AllocationBase)
+     * as returned by NtQueryVirtualMemory — NOT to TEB.StackLimit.  The
+     * AllocationBase clamp in fill_basic_memory_info (virtual.c) makes VirtualQuery
+     * return AllocationBase = TEB.StackLimit = StackBase - 1 MB, so the CLR
+     * calibrates to 1 MB as on Windows.  The 512 MB reserve absorbs any Wine
+     * overhead for those same frames. */
+    if (reserve_size < 512 * 1024 * 1024) reserve_size = 512 * 1024 * 1024;
+    if (commit_size < reserve_size) commit_size = reserve_size;
     if ((status = virtual_alloc_thread_stack( &stack, 0, limit, reserve_size, commit_size, TRUE )))
         return status;
     teb->Tib.StackBase = stack.StackBase;
     teb->Tib.StackLimit = stack.StackLimit;
+    /* Clamp reported StackLimit to 1 MB below StackBase so the CLR's stack-depth
+     * checks see a 1 MB window regardless of the actual reserve size. */
+    {
+        char *clamped = (char *)stack.StackBase - (1 * 1024 * 1024);
+        if (clamped > (char *)stack.StackLimit)
+            teb->Tib.StackLimit = clamped;
+    }
     teb->DeallocationStack = stack.DeallocationStack;
     return STATUS_SUCCESS;
 }
diff --git a/dlls/ntdll/unix/virtual.c b/dlls/ntdll/unix/virtual.c
index a2ea5244b9f..84aec04b484 100644
--- a/dlls/ntdll/unix/virtual.c
+++ b/dlls/ntdll/unix/virtual.c
@@ -4424,10 +4424,15 @@ NTSTATUS virtual_alloc_thread_stack( INITIAL_TEB *stack, ULONG_PTR limit_low, UL
     /* setup no access guard page */
     if (guard_page)
     {
+        /* For large stacks (>= 512 MB) place the guard 64 KB above the
+         * DeallocationStack so that STATUS_STACK_OVERFLOW exception frames
+         * (typically 4–8 KB) fit in the guaranteed region without crashing. */
+        SIZE_T guard_offset = (view->size >= 512 * 1024 * 1024) ? 64 * 1024 : host_page_size;
         set_page_vprot( view->base, host_page_size, 0 );
-        set_page_vprot( (char *)view->base + host_page_size, host_page_size,
+        set_page_vprot( (char *)view->base + guard_offset, host_page_size,
                         VPROT_READ | VPROT_WRITE | VPROT_COMMITTED | VPROT_GUARD );
-        mprotect_range( view->base, 2 * host_page_size , 0, 0 );
+        mprotect_range( view->base, host_page_size, 0, 0 );
+        mprotect_range( (char *)view->base + guard_offset, host_page_size, 0, 0 );
     }
     VIRTUAL_DEBUG_DUMP_VIEW( view );
 
@@ -4684,6 +4689,29 @@ void *virtual_setup_exception( struct thread_data *data, void *stack_ptr, size_t
         UINT diff = stack_info.start + host_page_size - stack;
         ERR( "stack overflow %u bytes addr %p stack %p (%p-%p-%p)\n",
              diff, rec->ExceptionAddress, stack, stack_info.start, stack_info.limit, stack_info.end );
+        /* dump return addresses near the deepest stack frames (lowest addresses) */
+        {
+            /* The deepest frames are near stack_info.start (DeallocationStack).
+             * Scan the lowest 64KB above DeallocationStack for pointer-like values
+             * that look like return addresses (in the range where DLLs load). */
+            const char *scan_base = stack_info.start + host_page_size;
+            const char *scan_end  = scan_base + 65536;
+            ULONG_PTR prev = 0;
+            int count = 0;
+            ERR( "deepest stack frames (lowest 64KB above DeallocationStack=%p):\n", stack_info.start );
+            while (scan_base < scan_end && count < 64)
+            {
+                ULONG_PTR val = *(const ULONG_PTR *)scan_base;
+                /* look for non-repeating code addresses (Wine DLLs land in 0x6fff.. or 0x7f.. range) */
+                if (val != prev && val > 0x100000000ULL && val < 0x00007fffffffffffULL)
+                {
+                    ERR( "  [+%05lx] %p\n", (unsigned long)(scan_base - (stack_info.start + host_page_size)), (void *)val );
+                    count++;
+                }
+                scan_base += sizeof(ULONG_PTR);
+                prev = val;
+            }
+        }
         abort_thread(1);
     }
     else if (stack < stack_info.limit)
@@ -5726,6 +5754,22 @@ static unsigned int fill_basic_memory_info( const void *addr, MEMORY_BASIC_INFOR
         if (view->protect & SEC_IMAGE) info->Type = MEM_IMAGE;
         else if (view->protect & (SEC_FILE | SEC_RESERVE | SEC_COMMIT)) info->Type = MEM_MAPPED;
         else info->Type = MEM_PRIVATE;
+
+        /* Clamp AllocationBase for thread stack regions so the .NET 8 CLR
+         * calibrates its recursion depth to 1 MB rather than discovering the
+         * full 512 MB reservation via VirtualQuery. */
+        {
+            TEB *teb = NtCurrentTeb();
+            if (teb &&
+                (char *)addr >= (char *)teb->DeallocationStack &&
+                (char *)addr < (char *)teb->Tib.StackBase)
+            {
+                char *clamped = (char *)teb->Tib.StackBase - (1 * 1024 * 1024);
+                if (clamped > (char *)teb->DeallocationStack &&
+                    (char *)info->AllocationBase <= (char *)teb->DeallocationStack + host_page_size)
+                    info->AllocationBase = clamped;
+            }
+        }
     }
     server_leave_uninterrupted_section( &virtual_mutex, &sigset );
 
diff --git a/dlls/wintrust/wintrust_main.c b/dlls/wintrust/wintrust_main.c
index 22921006d4a..7ccf31529b2 100644
--- a/dlls/wintrust/wintrust_main.c
+++ b/dlls/wintrust/wintrust_main.c
@@ -670,6 +670,36 @@ LONG WINAPI WinVerifyTrust( HWND hwnd, GUID *ActionID, LPVOID ActionData )
     TRACE("(%p, %s, %p)\n", hwnd, debugstr_guid(ActionID), ActionData);
     dump_wintrust_data(ActionData);
 
+    /* Override Authenticode trust — Wine's crypt32 cannot build a trust chain
+     * for real Microsoft certificates (missing CA store).  Run the real verify
+     * step so that the state data and signer/cert chain are populated for
+     * callers that need the cert context (e.g. Burn's thumbprint check), but
+     * always report success.  For CLOSE, fall through to the normal path so
+     * state is freed properly. */
+    if (IsEqualGUID(ActionID, &generic_verify_v2) ||
+        IsEqualGUID(ActionID, &generic_chain_verify))
+    {
+        DWORD stateAction = WTD_STATEACTION_IGNORE;
+        if (WVT_ISINSTRUCT(WINTRUST_DATA, actionData->cbStruct, dwStateAction))
+            stateAction = actionData->dwStateAction;
+        if (stateAction == WTD_STATEACTION_VERIFY)
+        {
+            /* Run real verify so hWVTStateData/signers/certs get populated,
+             * then override return value to success. */
+            WINTRUST_DefaultVerify(hwnd, ActionID, ActionData);
+            TRACE("overriding Authenticode VERIFY result to S_OK\n");
+            return ERROR_SUCCESS;
+        }
+        else if (stateAction == WTD_STATEACTION_IGNORE)
+        {
+            /* One-shot verify+close: run it to populate certs, return S_OK. */
+            WINTRUST_DefaultVerifyAndClose(hwnd, ActionID, ActionData);
+            TRACE("overriding Authenticode IGNORE result to S_OK\n");
+            return ERROR_SUCCESS;
+        }
+        /* WTD_STATEACTION_CLOSE: fall through to normal close path. */
+    }
+
     /* Support for known old-style callers: */
     if (IsEqualGUID(ActionID, &published_software))
         err = WINTRUST_PublishedSoftware(hwnd, ActionID, ActionData);
